Unresolved issues:
- When Subworkflow Output is placed in between two nodes, the output node fails during execution. (use preview image node as example)
- Subworkflow node shows progress, but it goes beyond the 100% mark, seems to go to 200%. (Check upscale workflow as example)
- Subworkflow Input transparancy for nodes is not always correct; for example when a concatenate string is used (check if my test was correct)
